/*
- boilerplate
    P1 fork boilerplate
    P1 setting-options-change repo name
    P1 setting-manage access - invite a partner

- boilerplate changes:
    .env.sample --> .env
    .env : change database name
           database url can be changed there
           random secret string
           send .env to the partner
    add readme
    .gitignore : .env !important⚠️⚠️
    app.js : routers

- Git branch creation
    git checkout -b develop
    git commit -m "update env and readme"
    git push origin develop

- Git flow
    P2 clone the repo of P1 without forking
    P2 changes on the project, and commit to the remote repo (github)

    P1 changes on the project, git add . & commit
    P1 git pull origin develop (merge codes from the remote repo), 
    P1 git push origin develop
    P1 add . & commit -m"solved merge conflict in xxx"

    
- 获取历史进度
    github-insight-nextwork

⚠️⚠️ when some weird situation happen, save commit and restart




*/