/*
-fs (file system) 文件系统
在Node中，与文件系统的交互是非常重要的，服务器的本质就是将本地的文件发送给远程的客户端
file system简单来说就是通过Node来操作系统中的文件
使用file system，需要先引入fs模块，fs是核心模块，直接引入，不需要下载

-同步和异步调用文件系统
同步：同步文件系统会阻塞程序的执行，除非操作完毕，否则不会向下执行代码
异步(sync)：异步文件系统不会阻塞程序的执行，在操作完成时，通过回调函数将结果返回
111
 */
var fs=require("fs");
console.log(fs);