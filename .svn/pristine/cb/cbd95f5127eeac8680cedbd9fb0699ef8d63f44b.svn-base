<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <script type="text/javascript">
    /*
    BOM (Browser Object Model) 
    浏览器对象模型
    BOM可以使我们通过JS来操作浏览器
    (DOM是操作网页的)

    BOM对象（图）
      window
          代表整个浏览器窗口，
          功能：同时window也是网页中的全局对象
      navigator
          代表当前浏览器的信息，
          功能：通过navigator识别不同的浏览器
      location
          代表当前浏览器的地址栏信息，
          功能：通过location获取地址栏信息，或操作浏览器跳转页面
      history
          代表浏览器的历史记录，
          功能：通过history来操作浏览器的历史记录
            ⚠️由于隐私原因，history不能获取到具体的历史记录，
            只能操作浏览器向前或向后翻页，且该操作只在当前次的访问有效
            （重新打开浏览器，则不能访问过去的历史记录）
      screen
          代表用户的屏幕的信息，
          功能：通过screen来获取用户显示器的相关信息
           ⚠️在做移动端时用得较多，浏览器网页基本不用，所以不会重点强调

      ⚠️navigator location history screen这些BOM对象在浏览器中都是作为window对象的属性保存的
      可以通过window对象使用，或是直接使用
         例： navigator或window.navigator
    */
    console.log(navigator);
    console.log(location);
    console.log(history);
    console.log(screen);
    </script>
</head>
<body>
    
</body>
</html>