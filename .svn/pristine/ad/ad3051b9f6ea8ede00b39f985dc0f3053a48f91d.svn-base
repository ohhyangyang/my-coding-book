<!doctype html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <title>Box Model 盒子模型</title>
        <style type="text/css">
        /*
        CSS中，认为每个元素都包含在一个个看不见的盒子里
        （参见视频47中的盒子模型图）或参见https://www.w3school.com.cn/css/css_boxmodel.asp
        element : 元素。（content）
        padding : 内边距，也有资料将其翻译为填充
        border : 边框
        margin : 外边距，也有资料将其翻译为空白或空白边
        width：element宽度
        height：element高度
        ⚠️width和height只是设置了盒子content的大小，盒子整个的大小由content，padding和border共同决定

        *border边框

        border的三个属性：
        border-width边框的宽度
        border-color边框的颜色
        border-style边框的样式
        ⚠️三个属性缺一不可

        -->border-width可以分别指定四个边框的宽度： 
        如果指定四个值：会分别设置为上 右 下 左
        如果指定三个值：会分别设置为上 左右 下
        如果指定两个值：会分别设置为上下 左右
        如果指定一个值：则四边都为该值
        ⚠️该规则适用于所有属性

        -->border-xx-width来设置指定边的宽度
           xx可以为top或right或bottom或left

        -->border-color设置四边边框颜色 规则同border-width

        -->border-xx-color设置指定边框颜色 规则同border-xx-width

        -->border-style设置四边边框样式 规则同border-width
        可选值
        none	无边框。
        dotted	点状边框。
        dashed	虚线
        solid	实线
        double	双线。双线的宽度等于 border-width 的值。
        （groove ridge inset outset 是一些3D效果）

        -->border-xx-style可以设置指定边框的样式，规则同border-xx-width

        
        

        */
        .box1{
            width:300px;
            height:300px;
            background-color: #bfa;/*护眼豆沙绿*/
            border-width:10px 20px 30px;
            border-bottom-width: 100px;
            border-color:cornflowerblue yellow orchid ;
            border-left-color:deeppink;
            border-style: double dashed dotted;
            

        }
        </style>


    </head>
    <body>
        <div class="box1"></div>

    </body>
</html>