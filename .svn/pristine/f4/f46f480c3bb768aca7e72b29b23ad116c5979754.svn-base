<!doctype html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <title>文档流</title>
        <style type="text/css">
        </style>
    </head>
    <body>
        <!--
            文档流 normal flow
            文档流处于页面最底层，表示页面的位置
            而所创建的元素都默认处在文档流中

            元素在文档流中的特点
            块元素
                1 在文档流中，会独占一行，自上向下排列
                2 在文档流中，默认width是父元素的100% (default value: auto)
                3 在文档流中，默认height被子元素内容撑开 (default value: auto)
            内联元素
                1 在文档流中，只占自身大小，从左向右排列，如一行装不下，则会换到下一行
                2 在文档流中，默认width和默认height被子元素内容撑开 (default value: auto)


            ⚠️当元素的width的值为auto时，此时设置padding不会影响可见框大小
            而是会自动修改width，以适应padding
        -->
        <div style="background-color: #bfa; padding-left:200px;">a</div>
        <div style="width: 100px;height: 100px;background-color: yellow;"></div>
        <span style="background-color:wheat">我是span</span>
        <span style="background-color:wheat">我是span</span>
        <span style="background-color:wheat">我是span</span>
    </body>
</html>